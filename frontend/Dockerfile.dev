# syntax=docker/dockerfile:1
# Development image: deps baked in, source mounted at runtime for HMR.
FROM node:22-slim
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host"]
